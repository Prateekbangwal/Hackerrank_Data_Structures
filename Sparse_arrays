#include<iostream>
using namespace std;
int main()
{
    int n,n1;
    string temp;
    string arr1[10000], arr2[10000];
    //cout<<"Enter the no of strings in array 1"<<"\n";
    cin>>n;

    for (int i = 0; i < n; i++)
        cin>>arr1[i];

    //cout<<"Enter the no of strings in array 2"<<"\n";
    cin>>n1;
         for (int i = 0; i < n1; i++)
        cin>>arr2[i];

    for(int i = 0;i<n1;i++)
    {
         int count =0;
        temp =arr2[i];
        for(int j =0;j<n;j++)
        {
            if(temp==arr1[j])
            {
                count++;

            }
            else{
                continue;
            }

    }
    cout<<count<<"\n";
    }
return 0;
}


